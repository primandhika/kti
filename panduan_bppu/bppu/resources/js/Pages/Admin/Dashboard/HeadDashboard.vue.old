<template>
  <AdminLayout page-title="Dashboard Pimpinan">
    <div class="space-y-6">
      <!-- Welcome Card -->
      <div class="bg-gradient-to-r from-[#6b4700] to-[#996600] rounded-2xl shadow-xl p-8 text-white">
        <h1 class="text-3xl font-bold mb-2">Dashboard Eksekutif</h1>
        <p class="text-[#d6c199]">Selamat datang, {{ $page.props.auth.user.name }}</p>
        <p class="text-sm text-[#d6c199] mt-1">Laporan dan analitik untuk pengambilan keputusan strategis</p>
      </div>

      <!-- Executive Stats Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <!-- Total Cash Flow -->
        <div class="bg-white rounded-xl shadow-md p-6 border border-gray-100 hover:shadow-lg transition-shadow">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-600">Total Kas Masuk</p>
              <p class="text-3xl font-bold text-gray-900 mt-2">{{ formatCurrency(totalIncome) }}</p>
            </div>
            <div class="w-14 h-14 bg-green-100 rounded-xl flex items-center justify-center">
              <svg class="w-7 h-7 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
          </div>
          <div class="mt-4 flex items-center text-sm">
            <span class="text-green-600 font-medium">{{ getPercentageChange() }}</span>
            <span class="text-gray-600 ml-2">dari bulan lalu</span>
          </div>
        </div>

        <!-- Total Expenses -->
        <div class="bg-white rounded-xl shadow-md p-6 border border-gray-100 hover:shadow-lg transition-shadow">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-600">Total Pengeluaran</p>
              <p class="text-3xl font-bold text-gray-900 mt-2">{{ formatCurrency(totalExpenses) }}</p>
            </div>
            <div class="w-14 h-14 bg-red-100 rounded-xl flex items-center justify-center">
              <svg class="w-7 h-7 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 9V7a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2m2 4h10a2 2 0 002-2v-6a2 2 0 00-2-2H9a2 2 0 00-2 2v6a2 2 0 002 2zm7-5a2 2 0 11-4 0 2 2 0 014 0z" />
              </svg>
            </div>
          </div>
          <div class="mt-4 flex items-center text-sm">
            <span class="text-red-600 font-medium">{{ totalExpenses > 0 ? '+' : '' }}{{ ((totalExpenses / (totalIncome || 1)) * 100).toFixed(0) }}%</span>
            <span class="text-gray-600 ml-2">dari pemasukan</span>
          </div>
        </div>

        <!-- Active Units -->
        <div class="bg-white rounded-xl shadow-md p-6 border border-gray-100 hover:shadow-lg transition-shadow">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-600">Unit Kerja Aktif</p>
              <p class="text-3xl font-bold text-gray-900 mt-2">{{ activeWorkUnits }}</p>
            </div>
            <div class="w-14 h-14 bg-purple-100 rounded-xl flex items-center justify-center">
              <svg class="w-7 h-7 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
              </svg>
            </div>
          </div>
          <div class="mt-4 flex items-center text-sm">
            <span class="text-gray-600">{{ totalWorkUnits > 0 ? ((activeWorkUnits / totalWorkUnits) * 100).toFixed(0) : 100 }}% operasional</span>
          </div>
        </div>

        <!-- Total Reports -->
        <div class="bg-white rounded-xl shadow-md p-6 border border-gray-100 hover:shadow-lg transition-shadow">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-600">Total Laporan</p>
              <p class="text-3xl font-bold text-gray-900 mt-2">{{ totalPosts }}</p>
            </div>
            <div class="w-14 h-14 bg-[#eae0cc] rounded-xl flex items-center justify-center">
              <svg class="w-7 h-7 text-[#996600]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-2m3 2v-4m3 4v-6m2 10H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
            </div>
          </div>
          <div class="mt-4 flex items-center text-sm">
            <span class="text-green-600 font-medium">+{{ monthlyIncome && monthlyIncome[0] ? monthlyIncome[0].transactionCount : 0 }}</span>
            <span class="text-gray-600 ml-2">bulan ini</span>
          </div>
        </div>
      </div>

      <!-- Financial Overview & Reports -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <!-- Monthly Cash Flow -->
        <div class="bg-white rounded-xl shadow-md p-6 border border-gray-100">
          <h3 class="text-lg font-bold text-gray-900 mb-4">Ringkasan Kas Bulanan</h3>
          <div class="space-y-4">
            <div
              v-for="(item, index) in monthlyIncome"
              :key="index"
              class="flex items-center justify-between p-4 rounded-lg"
              :class="{
                'bg-green-50': index === 0,
                'bg-blue-50': index === 1,
                'bg-purple-50': index === 2
              }"
            >
              <div class="flex items-center space-x-3">
                <div
                  class="w-10 h-10 rounded-lg flex items-center justify-center"
                  :class="{
                    'bg-green-600': index === 0,
                    'bg-blue-600': index === 1,
                    'bg-purple-600': index === 2
                  }"
                >
                  <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
                  </svg>
                </div>
                <div>
                  <p class="text-sm font-medium text-gray-900">Pemasukan {{ item.month }}</p>
                  <p class="text-xs text-gray-600">Total transaksi: {{ item.transactionCount }}</p>
                </div>
              </div>
              <p
                class="text-lg font-bold"
                :class="{
                  'text-green-600': index === 0,
                  'text-blue-600': index === 1,
                  'text-purple-600': index === 2
                }"
              >{{ formatCurrency(item.income) }}</p>
            </div>
          </div>
        </div>

        <!-- Recent Executive Reports -->
        <div class="bg-white rounded-xl shadow-md p-6 border border-gray-100">
          <h3 class="text-lg font-bold text-gray-900 mb-4">Laporan Eksekutif Terbaru</h3>
          <div class="space-y-4">
            <div class="flex items-start space-x-3 p-3 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors cursor-pointer">
              <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                </svg>
              </div>
              <div class="flex-1 min-w-0">
                <p class="text-sm font-medium text-gray-900">Laporan Keuangan Q1 2025</p>
                <p class="text-xs text-gray-500 mt-1">Disajikan 2 jam yang lalu</p>
              </div>
            </div>

            <div class="flex items-start space-x-3 p-3 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors cursor-pointer">
              <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                </svg>
              </div>
              <div class="flex-1 min-w-0">
                <p class="text-sm font-medium text-gray-900">Analisis Kinerja Unit Kerja</p>
                <p class="text-xs text-gray-500 mt-1">Disajikan 1 hari yang lalu</p>
              </div>
            </div>

            <div class="flex items-start space-x-3 p-3 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors cursor-pointer">
              <div class="w-10 h-10 bg-purple-100 rounded-lg flex items-center justify-center flex-shrink-0">
                <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 8v8m-4-5v5m-4-2v2m-2 4h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                </svg>
              </div>
              <div class="flex-1 min-w-0">
                <p class="text-sm font-medium text-gray-900">Proyeksi Anggaran 2025</p>
                <p class="text-xs text-gray-500 mt-1">Disajikan 3 hari yang lalu</p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Read-only Notice -->
      <div class="bg-blue-50 border border-blue-200 rounded-xl p-4">
        <div class="flex items-start space-x-3">
          <svg class="w-5 h-5 text-blue-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <div>
            <p class="text-sm font-medium text-blue-900">Mode Tampilan Eksekutif</p>
            <p class="text-xs text-blue-700 mt-1">Dashboard ini menampilkan data untuk keperluan analitik dan pengambilan keputusan. Akses terbatas pada tampilan laporan dan statistik.</p>
          </div>
        </div>
      </div>
    </div>
  </AdminLayout>
</template>

<script setup>
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Link } from '@inertiajs/vue3';

const props = defineProps({
  totalUsers: Number,
  totalWorkUnits: Number,
  activeWorkUnits: Number,
  totalBukuKas: Number,
  totalPosts: Number,
  totalIncome: Number,
  totalExpenses: Number,
  monthlyIncome: Array,
  roleDistribution: Object,
  activeSessions: Number,
});

// Format currency
const formatCurrency = (amount) => {
  if (amount >= 1000000) {
    return `Rp ${(amount / 1000000).toFixed(1)}M`;
  } else if (amount >= 1000) {
    return `Rp ${(amount / 1000).toFixed(0)}K`;
  }
  return `Rp ${amount}`;
};

// Calculate percentage change (simplified)
const getPercentageChange = () => {
  if (props.monthlyIncome && props.monthlyIncome.length >= 2) {
    const current = props.monthlyIncome[0].income;
    const previous = props.monthlyIncome[1].income;
    if (previous > 0) {
      const change = ((current - previous) / previous * 100).toFixed(0);
      return change > 0 ? `+${change}%` : `${change}%`;
    }
  }
  return '+0%';
};
</script>
